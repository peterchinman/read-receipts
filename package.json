{
	"name": "message-simulator",
	"version": "1.0.0",
	"description": "The iMessage experience, now in web form.",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "node --test",
		"format": "prettier --write \"**/*.{js,html,css}\" --plugin=prettier-plugin-embed",
		"format:check": "prettier --check \"**/*.{js,html,css}\"",
		"dev": "npx live-server --port=3000 --entry-file=index.html --ignorePattern=backend/"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"prettier": "^3.6.2",
		"prettier-plugin-embed": "^0.5.0"
	}
}
